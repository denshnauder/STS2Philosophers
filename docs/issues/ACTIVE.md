# 活动问题镜像

## ISSUE 009 西哲样例图未落实二轮设计审查

状态：处理中。D6停用WESTERN_EDGE_002、004、031、032、036并保留来源和ID；12旧样例分为8可执行、4仅来源。旧窗口与已接受节点保留回归、全部逻辑、Release、PCK及独立目录部署三哈希通过，未游戏验收。替代问号、独立语言入口、卢梭同人转问题与主题去重仍未接入，未闭环；六入口、七问题或89人基线不变。真源见13西哲路径修订。

> Obsidian `STS2 Mod 开发/08问题闭环/08问题与修改.md` 是用户查看活动问题、阶段结果和下一步的主要入口。
> 本文件只保存尚未闭环问题的轻量镜像，用于 Codex 跨窗口从 Git 恢复上下文，不能替代 Obsidian 更新。
> 已解决问题不得继续保留在活动列表中；优先归档到 Obsidian，仓库归档按需同步到 `docs/issues/resolved/`。

## 状态

- `待处理`：已记录，尚未开始。
- `处理中`：正在定位、修改或测试。
- `待验证`：代码修改已完成，但仍缺少足够验证，或需要游戏内/用户确认。
- `阻塞`：缺少用户决策、外部资源、环境条件或无法安全继续。
- `已解决`：不允许留在本文件；确认解决后立即归档。

## 执行原则

1. 优先继续 `处理中` 和 `待验证`，再处理 `待处理`。
2. 一次代码修改完成不等于问题解决；应继续执行与风险相称的测试、构建、日志检查、部署或其他验证。
3. 除非出现真正需要用户决策、外部资源或人工游戏验收的阻塞，否则不要在中间步骤停下等待新的指令。
4. 每个问题应先在 Obsidian 主视图持续维护：现象、目标、状态、根因、已做修改、验证结果、下一步；重要阶段再同步本镜像。
5. 修复过程中发现独立的新问题时，新增独立问题项，不把多个问题无限堆进同一条。
6. 问题确认解决后，优先迁移到 Obsidian `08问题闭环/已解决/`，仓库归档按需同步，并从两边活动列表删除。

---

## ISSUE 001 建立自然的 ChatGPT 到 Codex 持续开发工作流

**状态：** 待验证  
**优先级：** 高

### 问题

目前 ChatGPT、Google Drive 项目资料和本地 Codex 开发之间仍然比较割裂；同时 Codex 额度经常在工作中途耗尽，导致任务停在半途，新窗口不能立刻从准确状态继续。

### 期望方向

- 建立固定、唯一的 Codex 活动问题入口。
- Codex 开始 STS2Philosophers 开发任务时自动读取活动问题。
- Codex 持续推进“定位 → 修改 → 测试 → 复查”，不因完成一次代码修改就默认结束。
- 问题具有明确状态，并在工作过程中持续更新。
- 每完成一个可独立验证的小阶段就及时提交，并把“已做到哪里、下一步是什么、失败点是什么”写回活动问题。
- 即使 Codex 因额度、窗口或上下文限制中断，新 Codex 窗口也应只靠 `AGENTS.md`、`ACTIVE.md`、Git 状态和最近提交恢复工作，而不依赖聊天上下文。
- 尽量用自动测试、构建、日志或游戏内验收作为闭环依据。
- 已解决问题从活动列表迁移到归档，活动列表只保留仍需处理的内容。

### 已完成

- Google Drive 已建立 `08问题闭环`，包含活动问题与 `已解决` 归档目录。
- GitHub 仓库已建立本活动问题队列。
- 根目录 `AGENTS.md` 已加入启动读取、持续推进、验证和归档规则。

### 尚需验证

- 实际发生一次 Codex 中断后，新窗口能否无人工复述地继续。
- 是否需要进一步增加轻量的 `WORKLOG` / checkpoint 机制，避免单个 ISSUE 变得过长。

---

## ISSUE 002 赐福强度与思想约束失衡

**状态：** 待处理  
**优先级：** 高

### 问题

当前赐福的正面效果普遍较强，而对应的行为限制、机会成本、违背代价不明显；不同思想路线之间的净收益差异也较大。两者本质上是同一个问题：目前更多是在给“有主题的强力遗物”，还没有稳定形成“接受一种思想就必须承担一种行为约束”。

### 期望方向

- 不先做逐件数值微调，先建立统一的设计预算和约束框架。
- 每条路线至少明确：行为要求、正面回报、机会成本/违背代价、适配构筑、最坏滥用方式。
- 平衡比较应看“为了拿到收益需要改变多少正常打法”，而不只看遗物提供了多少数值。
- 允许部分思想更强，但必须对应更苛刻、更窄或更有风险的实践条件。
- 后续新增交叉/转派机制时，不能让玩家只挑各流派最强收益叠加。

### 验证目标

形成可自动或半自动检查的平衡基准，使明显失衡能在大量人工爬塔之前被筛出。

---

## ISSUE 003 人工验收负担过高且路线难以记忆

**状态：** 待处理  
**优先级：** 高

### 问题

现有 6 条路线、12 件遗物已经超过用户能轻松记忆和逐条完整爬塔验收的规模。用户没有足够时间通过大量实际游玩判断什么好用、什么不好用，也难以清楚记住“路线 → 人物 → 遗物 → 效果”的对应关系。

### 期望方向

- 把人工游戏测试从“主要验证方式”降为“最终体验抽查”。
- 增加可批量执行的机制模拟、场景测试或统计输出，尽量自动发现过强、过弱、触发率异常和死条件。
- 为所有思想路线建立游戏内或开发侧的清晰总览：人物、思想关键词、当前赐福、核心约束、后继/可能分支。
- 设计上减少必须背诵的隐藏关系，让事件 UI 本身解释“为什么这个人物现在出现、他和你当前思想是什么关系”。
- 后续讨论新的路线时优先使用统一的路线卡片/矩阵，而不是依赖记忆。

### 验证目标

用户无需记住全部内部结构，也能在游戏中理解当前思想状态；开发阶段无需完整跑完每条路线即可筛掉大部分机制和平衡问题。

---

## ISSUE 004 线性路线应改为随机邀请、交叉争鸣与第三层发展

**状态：** 处理中
**优先级：** 最高

### 问题

当前结构是第一层固定孔子/墨子/老子，选定一个根基后第二层只出现唯一固定后继，导致思想系统实际上是“选一条科技树一直升级”。这与项目期待的“思想之间发生碰撞、交叉、争鸣、修正甚至转派”不一致；同时第三层尚未实现。

### 期望方向

- 第一层思想家不再全部写死展示，改为从合法候选池随机出现若干邀请者。
- 随机不是纯 RNG，应结合角色、牌组、行为、已有思想状态或权重，使“思想家注意到玩家”有可解释性。
- 第二、第三层不再只有唯一纵向后继，而应允许出现至少三类关系：继承/深化、同题争鸣/修正、跨派邀请/转向。
- 第三层必须成为正式系统的一部分，用于会通、决裂、极端化、反驳或更高层次的思想重构，而不是简单再升级一次遗物。
- 不允许玩家无成本叠加多个流派的最优收益；转派、会通或接受对立思想应产生替换、冲突、代价或新的约束。
- 随机候选池可以同时承担部分强度平衡：强势赐福降低出现概率或提高青睐门槛，但不能用随机性掩盖根本数值失衡。

### 设计前置问题

- “思想关系”应成为图而不是树：需要定义人物之间的关系类型和可转移边。
- 需要决定第三层的功能：结论、会通、决裂还是一次最终论辩。
- 需要定义思想家青睐玩家的可解释条件，以及 UI 如何向玩家展示这些原因。

### 验证目标

一局游戏中的思想经历具有不确定性与叙事性：玩家可能坚持原说，也可能被新的思想吸引、修正、反驳或转向；不同局不再稳定走同一条固定路线。

### Phase 1 阶段结果

- 已审计第一层事件入口、第二层固定后继、局内存档字段、12 件遗物状态与青玉佩到熊掌的德继承；结构说明见 `DEVELOPMENT.md` 与 Obsidian 活动记录。
- 已建立 `CurrentDoctrine`、`ThoughtImprints`、`ActBehaviorStates`、`GeneratedCandidates` 的最小局内状态；思想印记目前只记录历史，不影响战斗。
- 第一层已改为孔子、墨子、老子中等概率随机展示两位；候选在进入事件前写入本局存档，载入后复用，拒绝路径保留。
- 已加入 `ThinkerProposal` 配置雏形，六个第一层提案从 `config/thinker_proposals.json` 读取；未实现的资格规则与共鸣标签明确留空。
- 已把六条“根遗物 → 固定后继”搬入 `LegacyRelicContinuationCandidateSource`；通用策略接受可替换候选源，但当前事件仍使用兼容源，第二层玩法未改变。
- 纯逻辑检查已覆盖三种候选组合的等概率、候选不重抽、四类局内状态编码往返、提案配置完整性和候选源可替换性；Release 构建为 0 警告、0 错误。
- 阶段提交：`0132304` 记录代码审计，`84d07d8` 实现第一层随机双候选与局内存档，`b424808` 加入提案配置并隔离固定后继候选源；均已推送到 `origin/main`。
- 验证结果：完整纯逻辑检查通过；`dotnet build -c Release -p:DeployOnBuild=false` 成功，0 警告、0 错误；嵌入资源名验证为 `STS2Philosophers.config.thinker_proposals.json`。
- 部署结果：确认游戏未运行后执行完整 Release 构建。PCK 打包首次遇到 Godot 编辑器设置的间歇性初始化错误，未改配置直接重试后成功；DLL、PCK 与清单已部署到本机 MOD 目录。
- 验收状态：已实现并通过自动检查，尚未启动游戏进行第一层候选显示与实际退出读档验收。
- 尚未实现且本阶段不处理：完整第二层行为候选、第三层、遗物数值调整，以及行为表达机会、遗物诱导、跨路线资源归属等未定设计。

### 下一步

- Phase 2A 第一步已完成行为记录数据模型：通用事实可累计，表达机会与行为印象分开保存并按单场去重，战斗中途状态可随现有局内存档往返；尚未接入战斗回调或第二层候选。
- 自动检查覆盖两场战斗累计、未表达机会、战斗中途往返与 Phase 1 旧载荷兼容；逻辑测试、Release 构建和 PCK 校验通过，部署哈希一致。
- Phase 2A 第二步已接入单人局全局战斗开始、出牌完成与战斗结束钩子，记录战斗生命周期、出牌总数和牌类型等中性事实；重复开始按恢复处理，新战斗会丢弃旧的未完成窗口。
- 第二步逻辑测试、Release 构建、PCK 校验和部署哈希复核通过；事实仍不生成印象或候选。
- Phase 2A 第三步已加入稳定排序的战斗结算诊断日志，可通过 `Behavior observation:` 检查每幕战斗数、事实、表达机会和印象；自动检查、构建、PCK 与部署复核通过。
- 战斗诊断日志已有游戏样本（9次出牌：6攻击、3技能）；当前仅剩修复后的新局读档链复验。首批“事实 → 表达机会/印象”规则、遗物诱导修正、跨路线资源和第三层设计继续保持未定。
- Phase 1 的游戏内“候选生成 → 退出 → 读档”验收仍待完成。

---

## ISSUE 005 退出读档后可重复领取涅奥奖励

**状态：** 待验证
**优先级：** 最高

### 问题与根因

- 实际游戏中完成涅奥奖励并进入“诸子观照”后退出，重新载入可再次领取一份涅奥奖励；存档中生成了两个独立的涅奥远古点记录。
- `NeowProceedPatch` 拦截原生继续流程后直接压入嵌套事件，没有先把底层涅奥房间标记为预完成并保存，导致房间栈恢复时涅奥仍可再次结算。
- 动态状态标记存在网络序列化警告，但本地 JSON 存档完整保留候选和行为统计，不是本次问题的直接根因。

### 已做修改与验证

- 只允许已经结束的涅奥事件进入拦截流程。
- 进入“诸子观照”前先调用 `MarkPreFinished`，并无条件保存涅奥完成状态与第一层候选，再压入嵌套事件。
- 回归检查覆盖未结束事件不拦截，以及“预完成 → 保存 → 进入诸子观照”的严格顺序。
- 纯逻辑测试全部通过；Release 构建成功，0 警告、0 错误。
- 2026年9月11日已重新运行全部逻辑测试、Release构建（0警告、0错误）、PCK打包与独立校验。确认游戏未运行后部署，DLL、PCK、清单SHA256全部一致。修复提交84dc4d0。

### 下一步

- 手动新开一局复验“领取涅奥奖励 → 进入诸子观照 → 退出 → 读档”，确认奖励不重复、两名候选不刷新。旧局已多领的奖励不会自动移除。

## 2026年9月11日恢复阶段

- ISSUE 001：明确真源为 `G:\我的云端硬盘\STS2mod\STS2 Mod 开发\08问题闭环\08问题与修改.md`；S盘同名旧库活动页改为导航，原文备份在 `.tools/note_backups/20260911/`。已修复本次读到旧进度的问题，跨后续窗口与云端工作流仍待验证。
- ISSUE 002、003：Obsidian 新增 `02想法与机制/09路线约束与验收基线.md`，提供六条路线映射、收益条件、机会成本、理论上界、风险场景和最小人工验收清单。仍缺实际平衡样本与预算决定，不标记闭环。
- 新增 `tools/VerifyMod.ps1`：使用本机配置的SDK，串联逻辑测试、DLL构建、无界面PCK打包、独立校验、游戏进程检查与部署哈希。已经完整执行通过，报告位于 `bin/Release/net9.0/verification.md`。
- 已同步Obsidian当天开发日志、赐福系统、工作优先级和项目主页；README明确读档修复尚待新局验收。
- 当前没有可由自动检查替代的游戏内读档结论；没有擅自选定哲学行为标签、改遗物数值或实施第三层。

## 遗留改动与流程收尾

- 用户授权处理此前两处改动：清单简介与当前玩法一致，Godot配置通过打包验证，均纳入本阶段提交；不再作为未处理遗留项。
- AGENTS.md固定启动、验证、笔记同步、审查、提交推送及最终状态检查。完整检查入口记录工作区差异，避免把混合产物等同于HEAD。
- 本阶段逻辑测试、Release构建（0警告、0错误）、PCK校验及部署哈希全部通过。README检查后无需改写，游戏内读档仍待人工验收。
- 新发现的校验报告残留问题已通过失败和成功路径验证，完整记录归档至Obsidian `08问题闭环/已解决/20260911 校验报告残留.md`。

## 自动接续配置

ISSUE 001增加当前任务内每四小时自动唤醒（sts2-2），Obsidian检查点为08问题闭环/02自动接续.md，仓库镜像为RESUME.md。配置已启用，首次自动唤醒及实际额度耗尽后的恢复尚待验收。当前已确认任务仍需人工读档或设计决定，无可执行步骤时不重复构建。

## ISSUE 007 西方哲学七路线独立发展与跨文明接口

**状态：** 处理中
**优先级：** 最高，ISSUE005人工验收独立等待。
**用户约束：** 七问题、六入口、89人用途；逐人物设计，西哲独立候选但共享主说与局内框架。苏格拉底两分支保留、单局一种；东哲、公开ModelId和历史边界不改。美术/制作成本前置；守灯仅实验，先审正式节点/界面/素材，不扩写正式场景或接生产。

**现在完成了什么：**
- 知识57可操作纸面原型有普通收入同条件对照、准确模拟副本的试授/收入/放弃、明确原句与后续材料追问。D95观察前后目标生命，单牌命中与整场胜利分开；33项及浏览器/明暗响应通过。
- 德性58有稳定回合首次动作前撤离、持久收益前缀代价、合法唯一后继及实际机会/标准/目的/自评/范围回看；32项及浏览器通过。程序不认证品德/范围限定。
- 19的玩家承诺和必要/充分推理错误已修；出处与游戏改写见63，同果异据及不能评分知识见66，最新观察见67。完整过程留10及开发日志，未将测试数量当设计完成。
- D86限定手动诊断socratescardprobe全逻辑/Release/PCK/部署哈希已通过；未在游戏取样，不能证明保存/加载/变形副本身份。旧杯/砝码及入口生产机制未替换；七路线未完整可玩。

- D115已读另一窗口cf1a41d最新用户制作约束，优先于旧人物扩写计划。74列正式节点/操作/界面/素材：普通战斗和专属两场仍需新增目标/设施场景，纯事件页会失去核心动作；停止守灯作主能力推进，仅保留HTML/检查及研究实验。约束从仍完整的活动真源恢复到接续/70/73，其他窗口规则提交保留。D116先比较现有卡牌出场安排与互斥用途改写，先成本/实例再判设计，不增新世界对象或美术/代码。

**准确下一步：** D116先比较现有卡牌出场安排/实际代价改序及互斥用途改写，明确节点/操作/界面/素材和具体例，再判断取舍/反例/哲学。守灯仅实验不扩写场景，不能先写能力/生成美术或因便宜复活旧被否决载体。

**独立待验证：** 原型体验、真实卡身份与非胜利退出、ISSUE005人工新局读档。未答不阻断独立设计；未经真实验证不接新能力生产，不自动启动游戏。原先额度耗尽已恢复，本次继续。

**归属与收尾：** HEAD阶段前9ece750，38笔待推，其中cf1a41d是另一窗口规则/制作约束提交且保留；阶段后按Git。D115只74/PlatoProductionReview、G70/73及当前设计/开发/活动镜像/索引；tmp/保留，无运行操作，无原型/游戏代码或资源改动。README生产准确，无新测试/构建/PCK/部署。G/S备份同步，ISSUE010无新证据不重试。

---

## ISSUE 010 GitHub TLS断开阻塞文档推送

**状态：** 阻塞
**优先级：** 高，独立于ISSUE007设计反馈。

现象：5ca31f8本地文档提交成功，Schannel推送两次均TLS握手断开；一次命令级OpenSSL后端尝试仍unexpected EOF。没有禁用证书验证或改全局Git配置，也未改生产代码。Git当前存在未推提交，后续本阻塞记录提交同样本地保存；未推列表按Git核实。

继续条件：有新的GitHub连接可用证据或用户告知网络恢复，再正常推送当前分支并核对origin/HEAD。仅定时唤醒或额度变化不重复推送，不循环重试；等待期间本地提交和G盘笔记保留。没有新增构建部署或游戏验收。
