# 苏格拉底行动观察

## D95实现前设计

D94已提交64e20e6，16笔未推，tmp/保留，无运行操作。知识29/德性32项及浏览器已通过，均为虚构纸面；未有新游戏构建/部署/人工验收。当前只保存观察设计，尚未改HTML。

### 问题与完整循环

九生命片段可操作地证明连击造成8伤害后仍有1生命，但完成记录只有伤害/目标/回合。后来基础攻击击倒，回看不能仅凭整场胜利判连击独自击倒。奖励承诺→实际行动→有限观察→原句与材料并列→收入/放弃与继续追问沿原规则，增加观察不增加权限或资源。

最小候选：现有每条实际hits附带hpBefore和hpAfter，均从该次命中前后准确目标读取；仅focus副本记录。不另建全战斗日志/事件协议，不保存推测伤害、未执行第二击或反事实“去掉牌会失败”。block仍记录grant与整回合aggregate，不宣称该副本独自防伤。

### 具体材料与反例

8生命目标：连击实际8→4、4→0；9生命目标：9→5、5→1，随后基础攻击击倒不追记为连击命中。4生命目标：4→0，目标死亡后第二击不执行。先用基础攻击再连击时读取实际当前剩余生命，不按fixture初始值造记录。两名敌人不串目标；同名额外副本不串focus。

回看只说“该次最后实际命中后此目标剩多少生命”，不说本场所有剩余状态由它造成。没有命中时不添目标观察；未抽/未用/纯格挡仍按各自问题。即使实际击倒也不证明必要、长期最优或知识。

### 恢复与检查边界

completed material深复制保留新增标量；后来enemyHp/material改动不能倒写，冻结查询不得持有可变嵌套对象。每个目标的相邻本副本命中应读取真实时序；不要求隔着其他动作也连续，不通过删无关动作凑连续。历史此前没有新字段时不猜数值（纸面刷新重开，本阶段不加持久迁移）。

实现只动trial HTML/知识两检查，检查8/9/4生命、基础攻击前置、两目标、纯格挡/未用、快照及同资源权限；浏览器九生命回看显示最后剩1、旧局部句未改与窄屏。相称纸面检查即可，README生产未变，无游戏构建部署。若观察被用来评分思想或自动制造反例，取消该逻辑。

准确下一动作：按以上设计给hits新增两个实际生命标量，inquiry只生成文字；完成相称检查、G活动/日志/镜像、备份单向同步S和逐文件提交。原生身份/退出、人工体验及ISSUE005/010各自独立等待，无新网络证据不重试推送。请求可执行仍继续，实际额度拒绝才等待接续。
