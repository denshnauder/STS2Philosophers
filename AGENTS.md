# 仓库开发规则

以下规则对以后所有开发窗口生效：

- 开始工作前必须阅读并遵守根目录的 `NAMING.md`。
- 新增或重命名内容前，先确定中文显示名、代码名、`ModelId`、本地化键和资源名。
- 禁止在自定义文件名和目录名中使用连字符 `-`。
- 未经用户当次明确要求，不运行 Godot、Steam、游戏本体、PCK 构建、PCK 校验或部署脚本。
- 默认只运行不启动游戏的纯逻辑测试，以及 `dotnet build -c Release -p:DeployOnBuild=false`。
- 游戏内描述遵循《杀戮尖塔 2》的简短风格，只写触发条件与实际效果。
- 不得在命名重构中顺便修改机制、数值或文案。
- 保留用户已有修改，不处理无关工作区变化。
- `ModelId` 公开后不得直接重命名。
- 如果用户的当次明确指示与这些规则冲突，以用户当次指示为准，并在结果中说明例外。

## Git规则

- 每个任务开始前必须检查Git状态。
- 必须保留用户和其他窗口已有的未提交修改。
- 不得覆盖、丢弃或回退与当前任务无关的改动。
- 日常任务只能暂存并提交本任务修改的文件。
- 只有用户明确要求时才能创建提交。
- 未经用户明确要求不得推送远端。
- 禁止使用 `git reset --hard`、`git clean` 或其他可能丢失工作内容的命令。
- 提交前必须完成与改动风险相称的构建和测试。
- 测试失败时不得建立声称功能完成的提交。
- 提交信息使用简短中文，不使用连字符。
- 如果开始任务时工作区不干净，先区分已有改动与本任务改动；无法安全区分时停止并报告。
