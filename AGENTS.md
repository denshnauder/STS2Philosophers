# 仓库开发规则

以下规则对以后所有开发窗口生效：

- 开始工作前必须阅读并遵守根目录的 `NAMING.md`。
- 开始工作前必须读取 Obsidian `STS2 Mod 开发/08问题闭环/08问题与修改.md`；仓库 `docs/issues/ACTIVE.md` 只作为 Codex 恢复上下文的轻量镜像。若其中存在未闭环问题，先恢复其状态和上下文，再处理用户当次任务；用户当次明确指定其他优先级时除外。
- 新增或重命名内容前，先确定中文显示名、代码名、`ModelId`、本地化键和资源名。
- 禁止在自定义文件名和目录名中使用连字符 `-`。
- 除非用户当次明确要求跳过，修改 C#、游戏内本地化、内容资源或构建相关文件后，自动完成与改动相称的构建与校验。
- C# 改动默认运行不启动游戏的纯逻辑测试，以及 `dotnet build -c Release -p:DeployOnBuild=false`；游戏内本地化或内容资源改动默认构建并校验 PCK。
- 构建与校验全部成功后，检查游戏进程；若游戏未运行，自动部署到 `local.props` 配置的 MOD 目录；若游戏正在运行，停止部署并报告。
- 始终不自动启动 Godot 编辑器界面、Steam 或游戏本体。
- 游戏内描述遵循《杀戮尖塔 2》的简短风格，只写触发条件与实际效果。
- 不得在命名重构中顺便修改机制、数值或文案。
- 保留用户已有修改，不处理无关工作区变化。
- `ModelId` 公开后不得直接重命名。
- 如果用户的当次明确指示与这些规则冲突，以用户当次指示为准，并在结果中说明例外。

## 问题闭环规则

- Obsidian `STS2 Mod 开发/08问题闭环/08问题与修改.md` 是面向用户的主要活动问题入口，只允许保留 `待处理`、`处理中`、`待验证`、`阻塞` 四种未闭环状态。
- `docs/issues/ACTIVE.md` 是便于 Codex 在新窗口从 Git 恢复上下文的轻量镜像，不是用户查看进展的主要入口，也不能替代 Obsidian 更新。
- 开始任何开发任务时，在检查 Git 状态后立即读取 Obsidian 活动问题页，并按需对照仓库镜像；优先继续 `处理中` 和 `待验证`，再处理 `待处理`。用户当次明确指定其他任务时，以当次指示为准，但仍需理解已有活动问题，避免冲突。
- 处理一个活动问题时，不得把“一次代码修改完成”视为问题解决。应在同一次可执行工作流中继续完成与风险相称的分析、修改、测试、构建、日志检查、部署检查和复查，直到满足验证条件或出现真实阻塞。
- 只有以下情况可以停止并标记 `阻塞`：需要用户做设计或体验决策；缺少无法自行获取的外部资源；必须人工启动游戏或执行当前规则禁止的操作；存在无法安全处理的工作区冲突；或当前环境确实无法继续。
- 每完成一个有意义步骤，必须优先更新 Obsidian 活动问题页中的状态、已定位原因、已做修改、验证结果和下一步；重要阶段再同步仓库镜像。不要只在聊天或 `docs/issues/ACTIVE.md` 中报告。
- 代码、构建或自动测试通过后，如果仍需要游戏内体验确认，状态应为 `待验证`，不能写成 `已解决`。
- 问题只有在验证条件满足后才能闭环。闭环时优先把完整记录迁移到 Obsidian `08问题闭环/已解决/YYYYMMDD 问题简称.md`；归档必须包含问题现象、期望目标、根因、最终修改、验证方法、最终结果、涉及文件与提交。若仓库镜像中存在该问题，再按需同步到 `docs/issues/resolved/`，随后从两边活动列表删除。
- 已解决问题不得长期留在任一活动文件中打勾或堆积；Obsidian 活动问题页必须始终保持为“现在还需要继续干什么”的主视图。
- 修复过程中发现独立的新问题时，新增独立 ISSUE，不把多个问题无限追加进同一条记录。
- 若 Obsidian 活动问题页为空，则按用户当次任务正常工作；任务中发现的新缺陷若无法在本阶段直接闭环，应先登记到 Obsidian，重要问题再同步仓库镜像。

## Git规则

- 每个任务开始前必须检查Git状态。
- 必须保留用户和其他窗口已有的未提交修改。
- 不得覆盖、丢弃或回退与当前任务无关的改动。
- 日常任务只能暂存并提交本任务修改的文件。
- 当一个可独立验证的功能阶段完成且相关测试通过后，及时创建独立提交，不等到多个阶段堆积。
- 阶段提交成功后，默认推送到 `origin` 的当前分支；用户当次要求不推送时例外。
- 只能提交本阶段可安全辨识的相关文件；若无法与用户或其他窗口的改动安全分离，则停止提交和推送并报告。
- 禁止使用 `git reset --hard`、`git clean` 或其他可能丢失工作内容的命令。
- 提交前必须完成与改动风险相称的构建和测试。
- 测试失败时不得建立声称功能完成的提交。
- 提交信息使用简短中文，不使用连字符。
- 如果开始任务时工作区不干净，先区分已有改动与本任务改动；无法安全区分时停止并报告。

## README规则

- `README.md` 是面向玩家和首次访问者的项目首页，优先介绍 Mod 世界观、核心乐趣、可玩路线、已实现内容、安装方法、兼容性与玩家需要知道的限制。
- 每次功能阶段完成后，在提交和推送前必须自动检查 `README.md` 是否仍与已验证的玩家体验一致。
- 如果本阶段改变了玩家可见机制、数值、事件流程、可玩内容、安装方式、兼容性或已知限制，必须在同一阶段提交中同步更新 `README.md`。
- 纯内部重构且玩家体验完全不变时，仍要检查 README，但不为了产生改动而强制改写。
- 构建、测试、PCK 校验、部署、调试命令和项目结构等开发者资料应写入 `DEVELOPMENT.md`，README 只保留简短的开发文档入口。
- README 只记录已经实现并经过验证的功能，不把尚未实现的构想写成现有功能。
- 未来设计、哲学整理与灵感讨论应写入对应笔记，不堆积在 README 中。

## 轻量工作流

- 小型、低风险改动优先复用已有代码、笔记和已确认结论，不重复进行全仓审计或 API 调查。
- 同一功能阶段内的相关微调集中完成，默认只在阶段末运行一次与风险相称的测试、构建和部署。
- 只有陌生 API、高风险存档或状态逻辑、跨分支冲突和公开接口迁移才单独进行专项审计。
- 每次任务把可复用的设计结论、原始资料和开发知识沉淀到对应的具体文件夹；避免冗长流水账和重复摘抄。
- 每个可独立验证的功能阶段在提交前必须同步 Obsidian 项目笔记：更新当天开发日志、受影响的 `02想法与机制` 页面，以及遗物变化涉及的 `07已有遗物`；不能只更新仓库 README。
- 笔记必须区分“已实现并通过自动检查”“已进游戏验收”和“候选构想”，不得把未实测内容写成已验收，也不得让旧方案继续冒充当前状态。
- 主协作窗口只保留决策、阻断和阶段结果；实现细节与长日志留在开发任务或文件中。
- 每个可独立验证阶段结束前，同时检查活动问题状态：若本阶段解决了 ISSUE，按问题闭环规则归档；若只完成代码但尚缺游戏内确认，保持 `待验证`。
